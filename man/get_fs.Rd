% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_fs_methods.R, R/get_fscore.R
\name{get_fs.data.frame}
\alias{get_fs.data.frame}
\alias{get_fs.default}
\alias{get_fs.lavaan}
\alias{get_fs.merMod}
\alias{get_fs}
\title{Get Factor Scores and the Corresponding Standard Error of Measurement}
\usage{
\method{get_fs}{data.frame}(
  data,
  model = NULL,
  group = NULL,
  method = c("regression", "Bartlett", "ML", "EB"),
  corrected_fsT = FALSE,
  vfsLT = FALSE,
  reliability = FALSE,
  format = c("unified", "list"),
  ...
)

\method{get_fs}{default}(data, ...)

\method{get_fs}{lavaan}(
  object,
  method = c("regression", "Bartlett", "ML", "EB"),
  corrected_fsT = FALSE,
  vfsLT = FALSE,
  reliability = FALSE,
  format = c("unified", "list"),
  ...
)

\method{get_fs}{merMod}(
  object,
  method = c("EB"),
  corrected_fsT = FALSE,
  vfsLT = FALSE,
  fsm = FALSE,
  format = c("unified", "list"),
  legacy_names = FALSE,
  ...
)

get_fs(data, ...)
}
\arguments{
\item{data}{A data frame, a fitted \link[lavaan:lavaan]{lavaan} model object, or a fitted
\link[lme4:lmer]{lme4::lmer} model object (\code{merMod}).}

\item{model}{An optional string specifying the measurement model
in \code{lavaan} syntax. Only used when \code{data} is a data frame.
See \code{\link[lavaan]{model.syntax}} for more information.}

\item{group}{Character. Name of the grouping variable for multiple group
analysis, which is passed to \code{\link[lavaan]{cfa}}.
Only used when \code{data} is a data frame.}

\item{method}{Character. Method for computing factor scores (options are
"regression" or "Bartlett"; "ML" is an alias for "Bartlett"
and "EB" is an alias for "regression"). Currently, the
default is "regression" to be consistent with
\code{\link[lavaan]{lavPredict}}, but the Bartlett scores
have more desirable properties and may be preferred for
2S-PA.}

\item{corrected_fsT}{Logical. Whether to correct for the sampling
error in the factor score weights when computing
the error variance estimates of factor scores.}

\item{vfsLT}{Logical. Whether to return the covariance matrix of \code{fsT}
and \code{fsL}, which can be used as input for \code{\link[=vcov_corrected]{vcov_corrected()}}
to obtain corrected covariances and standard errors for
\code{\link[=tspa]{tspa()}} results. This is currently ignored.}

\item{reliability}{Logical. Whether to return the reliability of factor
scores. Available only for single-factor lavaan models.}

\item{format}{Output format when \code{data} is a lavaan or merMod object.
\code{"unified"} (default) returns a single data frame with a \code{group} column;
for multiple groups, attributes \code{fsT}, \code{fsL}, \code{fsb}, and \code{scoring_matrix}
are named lists keyed by group label. \code{"list"} returns the legacy
shape: a named list of data frames (one per group) with per-group
matrix attributes. Use \code{\link[=fs_to_group_list]{fs_to_group_list()}} to convert between the two.}

\item{...}{additional arguments passed to \code{\link[lavaan]{cfa}}
(when \code{data} is a data frame). See \code{\link[lavaan]{lavOptions}}
for a complete list.}

\item{object}{A fitted model object.}

\item{legacy_names}{Logical. Random-effect score naming convention for
\code{merMod} objects. \code{FALSE} (default) uses \code{fs_u0}-style names
(\code{fs_u0}/\code{fs_u1}/..., with loadings \code{u0_by_fs_u0} and error terms
\code{ev_fs_u0}, \code{ecov_fs_u1_fs_u0}). \code{TRUE} reproduces the pre-refactor
\code{u0_eb}-style \emph{column names} (\code{u0_eb}, \code{u0_by_u0_eb}, \code{ev_u0_eb},
\code{ecov_u0_eb_u1_eb}) in the legacy column order. Note the legacy
output is name-compatible, not byte-identical, with the
pre-refactor \code{get_fs_lmer()} result: it additionally carries
score-error columns (\code{u0_eb_se}, ...), per-cluster \code{fsL}/\code{fsT}
array attributes, a per-cluster \code{scoring_matrix} list attribute
(see \code{\link[=get_fs]{get_fs()}}), and has NULL row names (the pre-refactor
output had none of these and used the ranef subject IDs as row
names).}
}
\value{
A data frame containing the factor scores (with prefix \code{"fs_"}),
the standard errors (with suffix \code{"_se"}), the implied loadings
of indicator \verb{_by_} factor scores, and the error variance-covariance
of the factor scores (with prefix \code{"ev_"} or \code{"ecov_"}).
For multi-group lavaan models in \code{"unified"} format, a \code{group} column
is included. The following attributes are attached:
* \code{fsT}: error covariance of factor scores (matrix or named list by group)
* \code{fsL}: loading matrix of factor scores (matrix or named list by group)
* \code{fsb}: intercepts of factor scores (vector or named list by group)
* \code{scoring_matrix}: weights for computing factor scores from the
observed data, as a named list. For lavaan models: one
score x item matrix per group. For \code{merMod} models: one
\code{num_re} x \code{n_j} matrix per cluster, where the cluster's EB
scores are \code{S_j \%*\% (y_j - X_j \%*\% beta)} with \code{y_j}/\code{X_j} the
cluster's rows of the model response and the fixed-effects
design.
}
\description{
\code{get_fs()} is an S3 generic that extracts factor scores from fitted models.
Methods are available for \code{data.frame} (fits a CFA internally), \code{lavaan}
objects, and \code{lmerMod} objects. Support for \code{mirt} models is planned.
}
\details{
When \code{data} is a data frame and \code{model} is supplied as a lavaan syntax string,
the function internally calls \code{\link[lavaan:cfa]{lavaan::cfa()}} and then dispatches to the
\code{lavaan} method. When \code{data} is a fitted model object, the appropriate S3
method is called directly.

\code{get_fs()} replaced \code{get_fs_lavaan()} and \code{get_fs_lmer()}, which are now
thin wrappers retained for backward compatibility.
}
\examples{
library(lavaan)
get_fs(PoliticalDemocracy[c("x1", "x2", "x3")])

# Multiple factors
get_fs(PoliticalDemocracy[c("x1", "x2", "x3", "y1", "y2", "y3", "y4")],
       model = " ind60 =~ x1 + x2 + x3
                 dem60 =~ y1 + y2 + y3 + y4 ")

# Multiple-group
hs_model <- ' visual  =~ x1 + x2 + x3 '
fit <- cfa(hs_model,
           data = HolzingerSwineford1939,
           group = "school")
get_fs(HolzingerSwineford1939, hs_model, group = "school")
# Or without the model
get_fs(HolzingerSwineford1939[c("school", "x4", "x5", "x6")],
       group = "school")
}
