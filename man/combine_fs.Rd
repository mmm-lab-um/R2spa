% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_fs.R
\name{combine_fs}
\alias{combine_fs}
\title{Combine \code{get_fs()} results into a block-diagonal multi-factor result}
\usage{
combine_fs(fs_list, ..., id = NULL, latent_names = NULL)
}
\arguments{
\item{fs_list}{A list of at least two \code{\link[=get_fs]{get_fs()}} results (lavaan, mirt, or
lme4; single- or multi-latent). Each element is one block (see
Details). Alternatively, the results may be passed as separate
arguments (cbind-style): \code{combine_fs(a, b, c)} is equivalent to
\code{combine_fs(list(a, b, c))}.}

\item{...}{Additional \code{\link[=get_fs]{get_fs()}} results (cbind-style). Use only with a
non-list first argument: \code{combine_fs(a, b, c)}, not
\code{combine_fs(list(a), b)}.}

\item{id}{A character string naming a column present in every input, used
to align the rows (see Details). Default \code{NULL} (same rows, same
order required).}

\item{latent_names}{Optional character vector. When supplied it must equal
the auto-derived global latent order (each input's latent names, in
\code{fs_list} order); a mismatch is an error. When \code{NULL} (default) the
names are derived from each input.}
}
\value{
A single-group data frame with one row per observation (marked
\code{per_obs = TRUE}). Columns, in order: the combined \verb{fs_<name>}
scores, the \verb{<name>_se} standard errors, the
\verb{<name_j>_by_fs_<name_i>} implied loadings, and the \code{ev_}/\code{ecov_}
error terms (all cross-block terms exactly zero). Attributes:
per-row \code{fsL}/\code{fsT}/\code{fsb}/\code{scoring_matrix} (block diagonal),
\code{fs_pattern}, the block-diagonal \code{psi}, the concatenated \code{alpha},
and \code{per_obs = TRUE}. The result feeds \code{\link[=tspa]{tspa()}} directly.
}
\description{
\code{combine_fs()} combines several \code{\link[=get_fs]{get_fs()}} results -- possibly from
different measurement models and different subsets of the data (for
example a \code{mirt} fit for some items together with a \code{lavaan} fit for the
rest) -- into a single \code{\link[=get_fs]{get_fs()}}-shaped result. The combined \code{fsL},
\code{fsT}, and \code{psi} attributes are \strong{block-diagonal across the inputs}: each
input contributes its own latent block (a multi-latent input contributes a
\verb{k x k} block with its internal structure intact), and every entry that
crosses from one input to another is exactly zero. The result is
downstream-transparent: it feeds \code{\link[=tspa]{tspa()}} directly (no explicit
\code{fsL}/\code{fsT} needed).
}
\details{
Each element of \code{fs_list} is one \strong{block}, so the block structure is
exactly the list structure. A two-factor \code{mirt} result is one \verb{2 x 2}
block (its two factors stay correlated, as estimated by \code{mirt}); two
separate single-factor fits are two \verb{1 x 1} blocks. Both ways of supplying
the \code{mirt} factors therefore work unchanged.

\strong{Row alignment.} With \code{id = NULL} (the default) every input must carry
the same number of rows in the same order -- the "different items, same
observations" case. With \code{id = "<column>"} the rows are aligned on that
column and the output rows are the \strong{union} of the inputs' \code{id} values in
first-appearance order; a row absent from input \emph{k} gets an \code{NA} score and
an all-\code{NA} block for that input's latents (the same NA-row convention
\code{mirt} uses for unscorable observations).

\strong{Derived columns.} The \verb{fs_<name>} scores and \verb{<name>_se} columns are
joined on \code{id}. The \verb{<name_j>_by_fs_<name_i>} implied-loadings and the
\code{ev_}/\code{ecov_} error columns are recomputed from the combined block-diagonal
\code{fsL}/\code{fsT}/\code{fsb}, so every cross-block loading and cross-block error
covariance is exactly zero (within-block values are the inputs' own).

\strong{\code{scoring_matrix}.} The combined \code{scoring_matrix} is a per-row \verb{q x p}
matrix, populated from the item-level scoring matrices of the inputs that
carry one (a complete-data \code{lavaan} result). Inputs without an item-level
matrix -- \code{mirt} (no item model in the result) and \code{lme4} (a per-cluster
random-effects matrix, not an item matrix) -- leave their rows as \code{NA}.
The attribute is always present (with \code{p = 0} when no input contributes
item columns).

\strong{Why not base \code{cbind()}.} \code{cbind()} on a data frame drops the \code{fsL} /
\code{fsT} / \code{psi} attributes, so it cannot build the block-diagonal
measurement inputs; \code{combine_fs()} is a distinct operation from the
single-factor column-join that the \code{\link[=tspa]{tspa()}} \code{se_fs} derivation is built
around.

\strong{Scope (v1).} Single-group inputs only: a multi-group result (a list of
per-group data frames, or a unified frame with a \code{group_col}) is rejected.
\code{lme4} inputs are one row per cluster, so align them on the cluster id.
}
\examples{
library(lavaan)
fit1 <- cfa("vis =~ x1 + x2 + x3", data = HolzingerSwineford1939)
fit2 <- cfa("qua =~ x4 + x5 + x6", data = HolzingerSwineford1939)
# list form and the cbind-style form are equivalent:
comb <- combine_fs(list(get_fs(fit1), get_fs(fit2)))
stopifnot(identical(comb, combine_fs(get_fs(fit1), get_fs(fit2))))
head(comb, 3)
# The psi attribute is block-diagonal (a vis block and a qua block with a
# zero cross block); the cross-block fsL / fsT entries are zero too:
attr(comb, "psi")

}
