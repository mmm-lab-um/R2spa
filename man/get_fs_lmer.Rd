% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_fscore.R
\name{get_fs_lmer}
\alias{get_fs_lmer}
\title{Get Factor Scores and the Corresponding Scoring Matrices for
Mixed-Effect Models}
\usage{
get_fs_lmer(
  object,
  method = c("EB"),
  corrected_fsT = FALSE,
  vfsLT = FALSE,
  fsm = FALSE,
  legacy_names = TRUE,
  ...
)
}
\arguments{
\item{object}{A fitted model object of class \link[lme4:lmerMod-class]{lme4::lmerMod}.}

\item{method}{\code{"EB"} (empirical Bayes, default) or \code{"ML"} (prior-free
per-cluster OLS), forwarded to \code{\link[=get_fs.merMod]{get_fs.merMod()}}.}

\item{corrected_fsT}{Currently not used.}

\item{vfsLT}{Currently not used.}

\item{fsm}{Currently not used.}

\item{legacy_names}{Logical. Passed to \code{\link[=get_fs.merMod]{get_fs.merMod()}}. Defaults to
\code{TRUE} so \code{get_fs_lmer()} keeps returning the pre-refactor
\code{u0_eb}-style \emph{column names} (in the legacy column order).
Note the legacy output is name-compatible, not byte-identical,
with the pre-refactor result: it additionally carries
score-error columns (\code{u0_eb_se}, ...), per-cluster \code{fsL}/\code{fsT}
array attributes, a per-cluster \code{scoring_matrix} list attribute
(see \code{\link[=get_fs]{get_fs()}}), and has NULL row names (the pre-refactor
output had no \verb{_se} columns, no attributes, and used the ranef
subject IDs as row names).}

\item{...}{Additional arguments, passed on to \code{\link[=get_fs]{get_fs()}}.}
}
\description{
\code{get_fs_lmer()} is superseded by \code{\link[=get_fs]{get_fs()}}. It is retained for backward
compatibility and delegates to \code{get_fs(object)} internally. New code should
call \code{\link[=get_fs]{get_fs()}} directly.
}
