% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tspa_corrected_se.R
\name{vcov_corrected}
\alias{vcov_corrected}
\title{First-order correction of sampling covariance for 2S-PA estimates}
\usage{
vcov_corrected(
  tspa_fit,
  vfsLT,
  which_free = NULL,
  engine = c("analytic", "fd"),
  ...
)
}
\arguments{
\item{tspa_fit}{A fit from \code{\link[=tspa]{tspa()}} with \code{fsT} and \code{fsL} supplied
(multi-factor measurement model, single- or multi-group),
so that it carries the \code{fsT}, \code{fsL}, and \code{tspa_args}
attributes. A fit corrected in place via
\code{tspa(corrected_se = TRUE)} (attribute
\code{tspa_corrected = TRUE}) is rejected.}

\item{vfsLT}{The sampling covariance matrix of the free \code{fsL}/\code{fsT}
elements, taken (or sub-matrixed) from the \code{vfsLT} attribute
of a \code{\link[=get_fs]{get_fs()}} result fitted with \code{vfsLT = TRUE}. Its row and
column order is the same \code{fsL}-then-\code{fsT} order described by
\code{which_free}. When \code{which_free = NULL} (the default) this is
the full square matrix returned by \code{get_fs()}; when
\code{which_free} is a subset of length \code{k}, this \strong{must} be the
matching \verb{k x k} principal submatrix, i.e.
\code{vfsLT_full[which_free, which_free]}.}

\item{which_free}{An optional numeric vector of positions selecting which
\code{fsL}/\code{fsT} elements to treat as free (and therefore to
propagate through the Jacobian). Positions run over the
\code{fsL} matrix (column-major order) followed by the
lower-triangular part of \code{fsT} including the diagonal
(column-major order). For a two-factor model \code{fsL} and
\code{fsT} are both 2 x 2: \code{fsL} occupies positions 1:4 and
\code{fsT} positions 5:7, so the two error variances are 5
and 7 and the error covariance between the two factor
scores (the \verb{[2, 1]} element of \code{fsT}) is position 6.
For a multigroup fit the positions run per group, in
group order: group 1's full \code{fsL} (column-major), then
group 2's full \code{fsL}, ..., then group 1's
lower-triangular \code{fsT} (column-major), then group 2's,
i.e. all loadings across groups first, then all
error-variance elements (matching the order of the
\code{vfsLT} attribute from \code{get_fs(vfsLT = TRUE)}).
\code{NULL} (the default) treats every per-group \code{fsL}/\code{fsT}
element as free. A non-\code{NULL} \code{which_free} of length
\code{k} requires \code{vfsLT} to be the matching \verb{k x k}
principal submatrix (see \code{vfsLT}).}

\item{engine}{The engine used to evaluate the Jacobian \code{J = d(thetahat)/d(eta)}. \code{"analytic"} (the default) uses a
refit-free, deterministic influence-function closed form
(PLAN 16, sections 2.4 and 4.3): \verb{J = -H^\{-1\} C}, with \code{H}
(the log-likelihood Hessian over the free params) and \code{C}
(the cross-derivative w.r.t. the fixed \code{fsL}/\code{fsT} entries)
obtained by central-differencing the analytic log-likelihood
score. It covers single- and multi-group models, saturated
and restricted (df > 0) structural models, and mean-structure
models, and is a pure function of the base fit + \code{vfsLT}
(bit-reproducible, no refits). \code{"fd"} uses central finite
differences (one stage-2 refit on each side of each free
element) and is retained as the A/B reference; the analytic
path transparently falls back to it only for a shape it cannot
handle (an unrecognised free parameter or unequal per-group
free-param counts). The two agree to the finite-difference
noise floor whenever \code{"analytic"} applies.}

\item{...}{Currently not used.}
}
\value{
A corrected covariance matrix in the same dimension as
\code{vcov(tspa_fit)} (symmetric).
}
\description{
First-order correction of sampling covariance for 2S-PA estimates
}
\details{
\code{vcov_corrected()} applies the first-order (delta-method) two-stage
approximation: the stage-2 covariance \code{vcov(tspa_fit)} is augmented by
\code{J \%*\% vfsLT \%*\% t(J)}, where \code{J} is the Jacobian of the stage-2
estimates with respect to the selected \code{fsL}/\code{fsT} free elements. With the
default \code{engine = "analytic"}, \code{J} is evaluated refit-free via a closed form
(see the \code{engine} argument below); with \code{engine = "fd"} it is instead
estimated by central differences, one full stage-2 refit on each side of each
free element while reusing the base fit's coefficients, so the cost is
\verb{2 x (number of free elements)} stage-2 refits.

The correction is \strong{partial by design}. It propagates only the \emph{sampling}
uncertainty of the stage-1 estimates of \code{fsL} (loadings/cross-loadings)
and \code{fsT} (score error variance-covariance, including its off-diagonal).
It does \strong{not} account for the sampling uncertainty of the factor-score
\emph{values} (the \verb{fs_<name>} columns fitted in stage 2), their standard
errors (\code{se_fs}), or the score intercepts (\code{fsb}); these are held fixed.
The two-stage approximation also treats the stages as independent, so the
cross-covariance between the stage-1 estimates and the stage-2 data is not
modelled.

The same correction can also be requested in place via
\verb{tspa(..., corrected_se = TRUE, vfsLT = <matrix>)}; \code{tspa()} then sets the
\code{tspa_corrected = TRUE} attribute on the returned fit, and
\code{standardizedSolution()} on that fit reports corrected standard SEs. Such
an already-corrected fit is rejected by \code{vcov_corrected()} (the
correction is never applied twice).

The \code{engine} argument selects how the Jacobian is evaluated. The default
\code{"analytic"} is refit-free and deterministic: an influence-function closed
form (PLAN 16, sections 2.4 and 4.3), \verb{J = -H^\{-1\} C}, with \code{H} (the
log-likelihood Hessian over the free params) and \code{C} (the cross-derivative
w.r.t. the fixed \code{fsL}/\code{fsT} entries) obtained by central-differencing the
analytic log-likelihood score. It covers single- and multi-group models,
saturated and restricted (df > 0) structural models, and mean-structure
models, and is a pure function of the base fit + \code{vfsLT} (bit-reproducible,
no refits). \code{"fd"} (central finite differences, one stage-2 refit per side
of each free element) is retained as the A/B reference; the analytic path
falls back to it only for a shape it cannot handle. The two agree to the
finite-difference noise floor whenever \code{"analytic"} applies.
}
\examples{
library(lavaan)

# Two-factor model, Bartlett scoring. The fsL/fsT free elements run over
# positions 1:7 (fsL -> 1:4, fsT -> 5:7); the two error variances are 5
# and 7, the score-error covariance is 6.
fs <- get_fs(PoliticalDemocracy,
             model = "ind60 =~ x1 + x2 + x3
                      dem60 =~ y1 + y2 + y3 + y4",
             method = "Bartlett", vfsLT = TRUE)
fit <- tspa("dem60 ~ ind60", data = fs,
            fsT = attr(fs, "fsT"), fsL = attr(fs, "fsL"))
vfsLT <- attr(fs, "vfsLT")

# Propagate only the two error variances (positions 5, 7): with a
# non-NULL which_free, pass the matching principal submatrix of vfsLT.
vc <- vcov_corrected(fit, vfsLT = vfsLT[c(5, 7), c(5, 7)],
                     which_free = c(5, 7))
sqrt(diag(vc))   # corrected standard errors
}
\seealso{
\itemize{
\item \code{vignette("Corrected Standard Errors", package = "R2spa")} for the corrected-SE workflow.
\item \code{vignette("Correction to Measurement Error", package = "R2spa")} for the underlying error correction.
}
}
