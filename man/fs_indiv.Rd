% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_indiv.R
\name{fs_indiv}
\alias{fs_indiv}
\title{Individual-specific (per-row) factor-score definition quantities}
\usage{
fs_indiv(fs, include_intercept = FALSE, ...)
}
\arguments{
\item{fs}{A factor-score object as returned by \code{\link[=get_fs]{get_fs()}} (or
\code{\link[=get_fs_lavaan]{get_fs_lavaan()}}/\code{\link[=get_fs_lmer]{get_fs_lmer()}}): a unified data frame, a named
list of per-group data frames (\code{format = "list"}), or a \code{merMod}
result (one row per cluster).}

\item{include_intercept}{Logical. When \code{TRUE}, also emit the factor
score intercepts (the \code{fsb} attribute) as \verb{int_fs_<f>} columns
(\code{q} of them). Ignored for \code{merMod} results, which have no \code{fsb}
(random effects are mean zero).}

\item{...}{Currently unused.}
}
\value{
A data frame with \code{nrow()} equal to the number of rows of the
input \code{\link[=get_fs]{get_fs()}} result (one row per observation for lavaan
models, one row per cluster for \code{merMod} models). Columns, in
order:
* the input's factor-score columns, kept unmodified (\verb{fs_<f>}; for
\code{merMod} models \verb{fs_u<k>}, or the legacy \verb{u<k>_eb} name when
\code{\link[=get_fs.merMod]{get_fs.merMod()}} was run with \code{legacy_names = TRUE});
* the per-observation standard errors (\verb{fs_<f>_se}; the square
root of the per-row \code{fsT} diagonal, \code{NA} where that entry is
negative or non-finite);
* the implied loadings of the latents on the factor scores
(\verb{<latent_j>_by_fs_<f>}, \code{q^2} of them);
* the error variances and error covariances of the factor scores
(\verb{ev_fs_<f>}, \code{q} of them, and \verb{ecov_fs_<a>_fs_<b>}, \verb{q choose 2} of them), in the same lower-triangular order \code{\link[=get_fs]{get_fs()}} uses;
* optionally (when \code{include_intercept = TRUE}) the score
intercepts (\verb{int_fs_<f>}, \code{q} of them);
* a trailing \code{group} column for multi-group lavaan results (the
input's own group-column name in \code{"unified"} format, or \code{group}
in \code{"list"} format); and
* a trailing \code{id} column holding the cluster/subject id for \code{merMod}
results.
}
\description{
\code{fs_indiv()} converts a \code{\link[=get_fs]{get_fs()}} result into a single long data frame
(one row per input row) carrying, for every observation, the
individual-specific standard errors, implied loadings, and error
variances and covariances of the factor scores, reusing \code{get_fs()}'s
column naming. In shape this resembles \code{mirt::fscores(full.scores.SE = TRUE)} (a per-observation table pairing each score with its standard
errors), but the quantities are read off the \code{fsL}/\code{fsT}/\code{fsb} attributes
of a \code{get_fs()} result rather than estimated from a new model.

For lavaan models the per-row values are pattern-resolved: an
observation's \code{fsL}/\code{fsT}/\code{fsb} depend only on its observed-indicator
pattern, not on its response values, so rows within a group differ only
when the data contain missing values (rows across groups may always
differ). For \code{merMod} models there is one row per cluster, each carrying
that cluster's own \code{fsL}/\code{fsT}.
}
\examples{
library(lavaan)
fit <- cfa("visual =~ x1 + x2 + x3", data = HolzingerSwineford1939)
# For a lavaan result the per-row se / loading / ev columns are the same
# ones get_fs() already carries; include_intercept = TRUE adds the score
# intercepts (int_fs_*) from the fsb attribute (non-NULL under mean
# scoring or with prior_mean/prior_cov).
fs_indiv(get_fs(fit, method = "mean"), include_intercept = TRUE)

# merMod: one row per cluster, with a trailing id column holding the
# cluster (Subject) level
library(lme4)
lmod <- lmer(Reaction ~ Days + (Days | Subject), sleepstudy)
fs_indiv(get_fs(lmod))
}
\seealso{
\itemize{
\item \code{vignette("Scoring Matrices: lavaan CFA and lme4", package = "R2spa")} for the scoring-matrix internals these per-row quantities come from.
}
}
